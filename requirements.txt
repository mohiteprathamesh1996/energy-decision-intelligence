# ─────────────────────────────────────────────────────────────────────────────
# Oil Supply Chain Optimizer — Python dependencies
#
# KEY CHANGE vs. Pyomo version:
#   pip install pulp   ←  bundles CBC solver automatically
#   No system-level coinor-cbc installation required.
# ─────────────────────────────────────────────────────────────────────────────

# Solver interface + bundled CBC
pulp==2.8.0

# Numerical computing
numpy==1.26.4
scipy==1.13.0

# Data processing
pandas==2.2.2

# Visualization & dashboard
plotly==5.22.0
streamlit==1.35.0

# Configuration
pyyaml==6.0.1

# ─────────────────────────────────────────────────────────────────────────────
# Development / testing  (pip install -r requirements-dev.txt)
# ─────────────────────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-cov==5.0.0
pytest-timeout==2.3.1
ruff==0.4.8
mypy==1.10.0

# ─────────────────────────────────────────────────────────────────────────────
# Solver upgrade path (no code changes needed, same PuLP interface):
#   pip install gurobipy          →  use pulp.GUROBI_CMD()
#   pip install cplex             →  use pulp.CPLEX_CMD()
#   pip install highspy           →  use pulp.HiGHS_CMD()
# ─────────────────────────────────────────────────────────────────────────────